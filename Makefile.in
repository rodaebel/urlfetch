PYTHON= @PYTHON@

all:
	@mkdir -p ebin
	@(cd src;$(MAKE))
	@(cd pyurlfetch; $(MAKE) PYTHON=$(PYTHON))

edoc:
	@(cd src;$(MAKE) edoc)

clean:
	@(cd src;$(MAKE) clean)

distclean: clean
	rm -f config.log Makefile
	rm -rf ebin
	@(cd pyurlfetch; $(MAKE) distclean)
